{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna run build && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^14.14.33",
    "lerna": "^4.0.0",
    "typescript": "^4.2.3"
  }
}
